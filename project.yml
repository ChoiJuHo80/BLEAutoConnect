name: BLEAutoConnect
options:
  bundleIdPrefix: com.yourname
  xcodeVersion: "15.0"
targets:
  BLEAutoConnect:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources: [Sources]
    info:
      path: Sources/Info.plist
