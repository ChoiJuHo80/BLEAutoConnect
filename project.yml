name: BLEAutoConnect
options:
  bundleIdPrefix: com.yourname
targets:
  BLEAutoConnect:
    type: application
    platform: iOS
    deploymentTarget: "15.0"
    sources: [Sources]
    info:
      path: Sources/Info.plist
